<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.springblade.modules.sp.mapper.MaterialListConversionMapper">

    <resultMap id="BaseResultMap" type="org.springblade.modules.sp.entity.MaterialListConversion">
        <id column="id" property="id"/>
        <result column="stream_id" property="streamId"/>
        <result column="branch_id" property="branchId"/>
        <result column="template_id" property="templateId"/>
        <result column="file_name" property="fileName"/>
        <result column="minio_url" property="minioUrl"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
        <result column="updated_at" property="updatedAt"/>
    </resultMap>

    <select id="getByTemplateStreamBranch" resultMap="BaseResultMap">
        SELECT id, stream_id, branch_id, template_id, file_name, minio_url, status, created_at, updated_at
        FROM material_list_conversions
        WHERE template_id = #{templateId} 
          AND stream_id = #{streamId} 
          AND branch_id = #{branchId}
    </select>

    <select id="getBatchByConditions" resultMap="BaseResultMap">
        SELECT id, stream_id, branch_id, template_id, file_name, minio_url, status, created_at, updated_at
        FROM material_list_conversions
        WHERE 
        <foreach collection="list" item="item" open="(" separator=" OR " close=")">
            (template_id = #{item.templateId} AND stream_id = #{item.streamId} AND branch_id = #{item.branchId})
        </foreach>
    </select>

    <update id="updateStatus">
        UPDATE material_list_conversions
        SET status = #{status},
            updated_at = CURRENT_TIMESTAMP
        WHERE id = #{id}
    </update>

</mapper>
