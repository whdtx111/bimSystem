<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.springblade.modules.sp.mapper.RevitAllMapper">

    <resultMap id="RevitAllResult" type="org.springblade.modules.sp.entity.RevitAll">
        <id property="id" column="id"/>
        <result property="objId" column="obj_id"/>
        <result property="templateId" column="template_id"/>
        <result property="elementId" column="element_id"/>
        <result property="streamId" column="stream_id"/>
        <result property="branchId" column="branch_id"/>
        <result property="commitId" column="commit_id"/>
        <result property="category" column="category"/>
        <result property="type" column="type"/>
        <result property="detail" column="detail" typeHandler="org.springblade.modules.sp.utils.JsonListTypeHandler"/>
        <result property="isfinished" column="isfinished"/>
        <result property="modifyUser" column="modify_user"/>
        <result property="modifyTime" column="modify_time"/>
        <result property="status" column="status"/>
    </resultMap>



    <select id="getById" resultMap="RevitAllResult">
        SELECT * FROM sp_revit_all WHERE id = #{id} and status != 9;
    </select>

    <select id="getRevitAllByCommitId" resultMap="RevitAllResult">
        SELECT * FROM sp_revit_all WHERE stream_id = #{streamId} and element_id = #{elementId} and commit_id = #{commitId} and status != 9
    </select>

    <select id="searchFilter" resultMap="RevitAllResult">
        SELECT * FROM sp_revit_all WHERE status != 9
        <if test="elementId != null">
            and element_id = #{elementId}
        </if>
        <if test="streamId != null">
            and stream_id = #{streamId}
        </if>
        <if test="branchId != null">
            and branch_id = #{branchId}
        </if>
        <if test="isfinished != null">
            and isfinished = #{isfinished}
        </if>
    </select>


    <insert id="addRevitAll" parameterType="org.springblade.modules.sp.entity.RevitAll">
        INSERT INTO sp_revit_all (id,template_id,obj_id, element_id, stream_id, branch_id, commit_id, category, type, detail, isfinished, modify_user, modify_time, status)
        VALUES (#{id},#{templateId},#{objId},#{elementId}, #{streamId}, #{branchId}, #{commitId}, #{category}, #{type}, #{detail, typeHandler=org.springblade.modules.sp.utils.JsonListTypeHandler}::jsonb, #{isfinished}, #{modifyUser}, #{modifyTime}, #{status})
    </insert>


    <!-- 更新记录 -->
    <update id="updateRevitAll" parameterType="org.springblade.modules.sp.entity.RevitAll">
        UPDATE sp_revit_all
        <set>
            <if test="elementId != null">
                element_id = #{elementId},
            </if>
            <if test="streamId != null">
                stream_id = #{streamId},
            </if>
            <if test="branchId != null">
                branch_id = #{branchId},
            </if>
            <if test="commitId != null">
                commit_id = #{commitId},
            </if>
            <if test="category != null">
                category = #{category},
            </if>
            <if test="type != null">
                type = #{type},
            </if>
            <if test="detail != null">
                detail = #{detail, typeHandler=org.springblade.modules.sp.utils.JsonListTypeHandler}::jsonb,
            </if>
            <if test="isfinished != null">
                isfinished = #{isfinished},
            </if>
            <if test="objId != null">
                obj_id = #{objId},
            </if>
            <if test="modifyUser != null">
                modify_user = #{modifyUser},
            </if>
            modify_time = #{modifyTime},
            status = #{status}
        </set>
        WHERE id = #{id}
    </update>

    <update id="updateIsFinished" parameterType="org.springblade.modules.sp.dto.RevitAllUPDTO">
        UPDATE sp_revit_all set obj_id = #{objId},isfinished = #{isfinished} where id = #{id};
    </update>

    <!-- 根据ID删除记录 -->
    <delete id="deleteRevitAllById" parameterType="String">
        DELETE FROM sp_revit_all WHERE id = #{id}
    </delete>

</mapper>
